# AI PR Review Assistant 演示讲稿

总时长控制在 15 分钟以内。建议节奏是：

- 1 分钟：开场和项目定位
- 3 分钟：问题、需求和解决思路
- 3 分钟：产品展示和 Timeline
- 5 分钟：架构、工程实现和核心设计
- 2 分钟：准确性控制、总结和扩展

---

## 第 1 页：AI PR Review Assistant

大家好，我今天分享的项目是 **AI PR Review Assistant**。

这是一个面向 GitHub Pull Request 的 AI 代码评审助手。它想解决的问题是：把开发者在第一轮 Review 中经常要做的事情，也就是读 diff、找风险、写建议，变成一个可视化、可追踪、可约束的 AI 辅助流程。

这个项目不是简单地把整个 diff 丢给模型，然后让模型输出一段总结。我的目标是把 AI Review 做成一个有输入、有过程、有依据、有结果的工作台。

技术栈上，前端使用 React + TypeScript，后端使用 Node.js，数据来自 GitHub API，实时过程通过 SSE 推送，模型分析部分通过 LLM Review Loop 来控制。

一句话概括：

> 我做的不是“AI 总结器”，而是一个“受控的 AI Review 助手”。

---

## 第 2 页：为什么做

我先讲为什么要做这个项目。

在真实开发里，PR Review 的第一轮成本很高。Reviewer 不是一上来就能判断哪里有问题，而是要先理解这个 PR 改了什么、涉及哪些文件、哪些地方可能有风险。

这里有几个痛点。

第一，变更上下文很分散。Reviewer 需要在标题、描述、文件列表和 diff 之间来回切换，先手动筛重点。

第二，大 PR 文件很多，关键风险容易被大量低价值变更淹没，比如 lock 文件、构建产物、格式化改动。

第三，Review 质量依赖个人经验和当时状态。时间紧或者上下文不熟时，就容易漏掉异常处理、API 边界、数据模型这类问题。

第四，如果直接让 AI 总结 PR，结果又容易变成黑盒。它可能说有风险，但用户不知道它看了哪些文件，也不知道依据是什么。

所以有价值的 Review 不能只给结论，还必须给出风险、原因和建议。

---

## 第 3 页：真实需求

开发者真正需要的不是一个“AI 总结”，而是一个可信的 Review 助手。

这里的可信，主要体现在五个方面。

第一，它要能帮助我快速理解 PR 范围。也就是这个 PR 改了什么、影响哪些模块、变更规模有多大。

第二，它要能优先识别高风险文件，而不是平均地看所有文件。比如接口层、鉴权逻辑、数据模型、异常处理和删除逻辑，通常比构建产物更值得看。

第三，每个风险都要有 diff 依据。不能只是说“这里可能有问题”，而要说明问题来自哪个文件、哪类变更。

第四，建议要能转化成 Review comment。也就是说，建议不能空泛，要尽量具体到可以让开发者修改。

第五，AI 的分析过程要可追踪、可复核。用户能看到 AI 看了什么、为什么看、怎么得出结论。

所以这个项目的核心需求不是“让 AI 多说一点”，而是“让 AI 的判断更可验证”。

---

## 第 4 页：解决思路

我的解决思路是：把 AI Review 从“一次性回答”改成“受控流程”。

它的流程可以拆成五步。

第一，用户输入 GitHub PR URL。

第二，后端获取 PR 元数据和 changed files。

第三，系统构建 Review Context，把原始 GitHub 数据整理成模型更容易消费的上下文。

第四，AI 不是一次性生成最终报告，而是多轮选择文件、识别风险、生成建议。

第五，前端实时展示 Pipeline 和 Agent Timeline。

这里最关键的设计思想是：模型负责分析，系统负责上下文、状态、边界和校验。

也就是说，我不是完全相信模型，而是把模型放进一个后端控制的流程里。每一步都能展示给用户，也能被系统校验。

核心原则就是这一页上的四个词：

> 可视化、可追踪、可复核、可约束。

---

## 第 5 页：产品展示

这一页是产品主界面。

用户输入一个 GitHub PR URL 后，会进入一个完整的 Review 工作台。

左侧是 Pull Request 信息，包括标题、作者、分支、状态、增删行和文件列表。这部分帮助用户先理解 PR 的基本情况。

中间是 Diff Viewer，可以按文件查看真实 patch，并且区分新增、删除和 hunk。这样最终的 AI 结论可以和原始 diff 对照起来看。

右侧是 AI Review 区域，包括 Pipeline 状态、Agent Timeline、Context 统计、风险和建议。

这个布局的目标是把“输入、证据、过程、结果”放在同一个工作台里。

所以用户不是只看到一个最终报告，而是可以从右侧结果回到中间 diff，再回到左侧 PR 信息，形成完整证据链。

---

## 第 6 页：AI Review Timeline

这一页讲 AI Review Timeline。

我希望 AI 的判断过程是可见的，所以没有让模型直接输出一段最终总结，而是把它拆成四类动作。

第一类是 `inspect_files`，表示模型选择要检查的文件，并说明为什么要看这些文件。

第二类是 `record_risk`，表示模型基于已检查的 diff 记录一个风险，包括风险等级、类型和依据。

第三类是 `record_suggestion`，表示模型针对已有风险生成建议。

第四类是 `finish`，表示审查结束，输出总结和整体风险等级。

这些动作会实时显示在右侧 Timeline 中。

这样做的价值是，Reviewer 能看到 AI 到底看了什么、为什么看、有没有直接跳结论，以及最终结论是怎么一步步形成的。

如果 AI 判断有问题，用户也能从 Timeline 里看出它在哪一步走偏。

---

## 第 7 页：系统架构

这一页是系统架构图。

整体链路是从 PR URL 到 AI Review。

最左侧是 Web 前端，负责用户输入、diff 展示、timeline 展示和最终结果展示。

中间是 Node.js 后端。后端主要做三件事：第一，调用 GitHub API 获取数据；第二，构建 Review Context；第三，编排 Review Pipeline。

右侧是 GitHub API 和 LLM Review Loop。

GitHub API 提供 PR metadata、changed files 和 patch。

LLM Review Loop 负责结构化 action 分析。模型每一轮只输出一个 action，比如 `inspect_files`、`record_risk`、`record_suggestion` 或 `finish`。

最后，后端通过 SSE 把阶段、心跳、AI 动作和结果实时推回前端。

这张图想表达的是：

> AI 不是直接接管系统，而是被放在一个由后端控制的 Review Pipeline 里。

---

## 第 8 页：工程实现

这一页讲具体工程实现。

第一步是 PR URL 解析。系统会校验 GitHub PR URL 格式，并提取 `owner`、`repo` 和 `pullNumber`。

第二步是 GitHub 数据获取。后端会获取 PR 的 title、description、author、branch、增删行、文件列表和 patch。

第三步是 Review Context 构建。这里会整合 PR 信息、文件 diff、统计数据和 warnings。

第四步是 SSE 流式反馈。后端会持续推送 `fetch metadata`、`fetch files`、`build context`、`review loop`、`result` 等阶段。

第五步是前端工作台展示。前端会同步展示 PR 信息、Diff Viewer、AI Timeline、风险和建议。

这里我想强调的是：项目不是前端单页调用一个 AI 接口，而是有完整的数据获取、上下文整理、AI 流程编排和实时可视化。

---

## 第 9 页：核心设计：上下文构建

这一页讲第一个核心设计：Review Context。

我的原则是：

> 先治理输入，再交给模型。

因为 GitHub 原始数据里包含很多噪音，如果直接塞给模型，模型既容易浪费上下文，也容易被低价值内容干扰。

所以 Review Context 会保留几类关键内容。

第一类是 PR 信息，包括标题、描述、作者、分支和状态。

第二类是文件信息，包括文件名、状态、增删行和 patch。

第三类是统计信息，比如 totalFiles、includedFiles、ignoredFiles 和 largeChange。

第四类是 truncated、ignored 和 warning 标记。

上下文策略主要有三点。

第一，过滤 lock、dist、build、minified 文件。

第二，单文件 patch 超长时做截断。

第三，大 PR 会标记 warning，让模型优先做风险文件筛选。

目标就是减少噪音、控制 token、保留关键证据。

---

## 第 10 页：核心设计：受控 Review Loop

这一页讲第二个核心设计：受控 Review Loop。

每一轮模型只允许输出一个 JSON action。

比如这一页展示的是 `record_risk` action，它包括文件名、风险等级、标题和原因。

系统只允许四类动作。

第一，`inspect_files`，选择文件。

第二，`record_risk`，记录风险。

第三，`record_suggestion`，生成建议。

第四，`finish`，结束评审。

这样设计的价值是，把一次黑盒回答拆成多个可验证步骤。

后端可以校验每一步是否合法。比如，模型不能给未检查过的文件记录风险，suggestion 必须绑定已有 risk，riskLevel 也必须是固定枚举。

所以 Review Loop 的本质不是让模型“自由发挥”，而是让模型在一个协议内工作。

---

## 第 11 页：准确性与误报漏报控制

这一页讲准确性控制。

我这里的观点是：准确性不只靠 prompt，而是靠系统约束。

降低误报主要靠四个规则。

第一，风险必须来自已 inspect 文件。

第二，风险必须由 diff 支撑。

第三，suggestion 必须绑定已有 risk。

第四，忽略低价值文件，减少噪音。

降低漏报主要靠五个策略。

第一，小 PR 尽量检查全部文件。

第二，大 PR 优先检查高风险文件。

第三，重点关注安全、鉴权、API 边界、数据模型、异常处理和删除逻辑。

第四，如果模型第一次检查后直接 0 风险结束，后端会触发二次复查。

第五，如果有风险但没有建议，后端会强制补充建议。

同时系统有硬限制：最多 10 轮、6 个风险、6 个建议、单轮 7 个文件。

这些限制不是为了让模型更聪明，而是为了让输出进入可控范围。

---

## 第 12 页：总结与未来扩展

最后总结一下。

当前这个项目已经实现了六件事。

第一，GitHub PR URL 解析。

第二，PR 元数据与 patch 获取。

第三，Review Context 构建。

第四，多轮 AI Review Loop。

第五，SSE 实时 Timeline。

第六，风险识别与 Review 建议生成。

未来我会优先扩展几个方向。

第一，行级 GitHub 评论写回，让建议可以直接落到具体代码行。

第二，Follow-up Chat，让用户基于同一个 Review Context 继续追问。

第三，仓库代码索引与跨文件上下文，让 AI 不只看 diff，也能理解相关代码。

第四，接入 CI、测试和覆盖率信息，提高判断依据。

第五，支持团队 Review Policy，让不同团队可以配置自己的审查规则。

第六，按 PR 大小和风险类型路由不同模型，平衡速度、成本和质量。

最后用一句话收尾：

> AI 不替代工程师判断，而是帮助工程师更快找到需要判断的地方。
