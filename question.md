# AI PR Review Assistant 面试题库

下面整理的是围绕这个项目最常被追问的一组问题，回答尽量按“先结论，再解释，再落到实现”的方式组织。

## 1. 你的项目本质上解决的是什么问题？它和普通 AI 总结器有什么区别？

**回答要点：**

- 不是做“输入 PR URL，输出一段总结”的工具，而是把 PR Review 做成一个**受控、多轮、可追踪**的流程。
- 普通总结器是一轮输出，容易黑盒，结果不稳定，也难以复核。
- 我的项目把 review 拆成多个动作：`inspect_files`、`record_risk`、`record_suggestion`、`finish`。
- 这样做的价值是：
  - 过程可见
  - 结果可校验
  - 风险有依据
  - 建议能回溯到具体 diff

**一句话版：**

> 普通 AI 总结器是一次性回答，我这个项目是把 AI review 变成一个受控的工作流。

---

## 2. 你的项目和 agent 的联系和区别是什么？

**回答要点：**

- 它具备 agent 的基本闭环：观察信息、做决策、执行动作、再观察结果。
- 但它不是通用 agent，因为我没有让它自由规划目标，也没有让它自由调用工具。
- 它被限定在一个非常窄的 review 场景里，只能执行少量白名单动作。
- 这样做是为了稳定性、可校验性和可解释性。

**一句话版：**

> 它像一个受控的垂直 agent，而不是一个开放式通用 agent。

---

## 3. 为什么不能把整个 diff 一次性丢给模型？

**回答要点：**

- PR 可能很大，直接喂给模型会占用大量上下文。
- 大量无关信息会淹没重点，模型容易被噪音带偏。
- diff 里常混着格式化、重命名、构建产物、依赖变更，价值密度不一样。
- review 的目标不是复述所有变更，而是找出高风险点。

**一句话版：**

> 一次性全塞给模型，问题不是“看不完”，而是“看不准”。

---

## 4. 后端怎么做校验和约束？

**回答要点：**

- 模型输出必须是固定 JSON 结构。
- 后端先做 schema 校验，再做语义校验。
- 校验重点包括：
  - `type` 是否合法
  - 字段是否完整
  - `suggestion` 是否绑定已有 `risk`
  - `inspect_files` 是否来自 PR 文件候选集
  - `risk` 是否能在 diff 中找到依据
- 如果输出非法，直接重试或失败。
- 如果格式合法但内容不可信，不进入最终结果。

**一句话版：**

> 模型负责生成候选结果，后端负责判断它能不能算有效 review。

---

## 5. `inspect_files` 的候选集是怎么筛出来的？

**回答要点：**

- 先由后端做初筛，不让模型从全量文件自由挑选。
- 初筛规则主要看：
  - 文件路径和职责
  - 变更规模
  - 风险特征
- 优先保留业务逻辑、接口层、核心服务、鉴权、数据模型等文件。
- 默认过滤 `lock`、`dist`、`build`、`minified` 这类低价值文件。
- 大 PR 会带 warning，让模型优先看高风险文件。

**一句话版：**

> 不是让模型自己找重点，而是先用系统把重点筛出来。

---

## 6. 为什么把 `record_risk` 和 `record_suggestion` 分开？

**回答要点：**

- `record_risk` 解决的是“哪里有问题、为什么有问题”。
- `record_suggestion` 解决的是“针对这个风险怎么修”。
- 两者拆开后，流程更清晰，也更容易做后端校验。
- 如果合在一起，模型容易先想方案再倒推风险。
- 拆分以后，review 的链路更像人工审查：先判断，再建议。

**一句话版：**

> 先识别风险，再生成建议，能减少黑盒感，也更利于校验。

---

## 7. `Review Context` 为什么要先建，而不是直接把 PR 信息传给模型？

**回答要点：**

- 原始 PR 信息太杂，包含 metadata、文件列表、patch、统计信息和 warning。
- 先构建 `Review Context`，是为了把原始输入变成模型更容易处理的结构化上下文。
- 后端会先过滤低价值文件、截断超长 patch、加 warning。
- 这样可以减少噪音，控制 token，也让模型更聚焦高风险点。

**一句话版：**

> 先治理上下文，再交给模型，是为了让模型面对的是“可审查的信息”，而不是原始噪音。

---

## 8. 为什么不保留全部 patch？

**回答要点：**

- 不追求“信息越多越好”，而是追求“对 review 有效的信息尽量完整”。
- 构建上下文时会：
  - 过滤低价值文件
  - 对超长 patch 做截断
  - 对大 PR 加 warning
- review 目标不是复述所有改动，而是识别高风险点和关键变更。

**一句话版：**

> 上下文不是越大越好，而是越可用越好。

---

## 9. `heartbeat` 的作用是什么？

**回答要点：**

- 这是给前端的“任务仍在运行”的信号。
- 长任务期间如果没有反馈，用户会以为系统卡死。
- 后端通过定时器周期性发送 `heartbeat`。
- 前端据此更新运行状态和耗时。
- 这能减少用户重复点击和误判失败。

**一句话版：**

> `heartbeat` 是长任务可观测性的一部分，不是装饰字段。

---

## 10. 如果 `runReviewLoop` 中途报错，怎么处理？

**回答要点：**

- 每轮输出都要先过格式和动作白名单校验。
- 非法 action 不会被当成有效结果。
- 如果中途失败，会记录已完成步骤并终止或重试。
- 允许单轮失败，但不允许流程失控。
- 如果连续多轮都不可信，就收口为失败或人工复核。

**一句话版：**

> 允许模型犯错，但不允许错误穿透到最终结果里。

---

## 11. 为什么 `risk` 和 `suggestion` 要关联，而不是独立输出？

**回答要点：**

- `risk` 和 `suggestion` 是两个不同层次的问题。
- `risk` 先回答“哪里有问题”，`suggestion` 再回答“怎么修”。
- 通过 `riskId` 绑定，可以校验建议是否真的对应一个确认过的风险。
- 这样可以避免建议漂移、空泛生成和先有结论后倒推风险。

**一句话版：**

> 建议必须服务于已确认的风险，不能脱离上下文单独存在。

---

## 12. 这个项目和普通工作流有什么区别？

**回答要点：**

- 普通工作流更多是固定流程执行。
- 我的项目把 AI review 过程做成了可控、可追踪、可解释的链路。
- 它不是“工作流 + 一个模型”，而是：
  - 数据获取
  - 上下文治理
  - 受控推理
  - 实时可视化
  的完整闭环。

**一句话版：**

> 它不是普通工作流加 AI，而是把 AI review 本身产品化、流程化、可审计化了。

---

## 13. SSE 是怎么实现的？

**回答要点：**

- 前端通过 `EventSource` 发起连接。
- 后端 `/review/stream` 接口返回 `text/event-stream`。
- 再设置：
  - `Cache-Control: no-cache, no-transform`
  - `Connection: keep-alive`
  - `flushHeaders()`
- 后端通过 `res.write('data: ...\\n\\n')` 持续推送事件。
- 事件包括：
  - `step`
  - `heartbeat`
  - `loop_action`
  - `result`
  - `done`

**一句话版：**

> 这是标准的 SSE 长连接：先发响应头，再持续写事件流。

---

## 14. 为什么用 SSE，而不是轮询或 WebSocket？

**回答要点：**

- 这是单向推送场景，前端主要是接收状态，不需要双向通信。
- 比轮询省请求、低延迟、体验更稳定。
- 比 WebSocket 更轻量，接入成本更低。
- 浏览器原生支持断线重连，适合长任务进度展示。

**一句话版：**

> 这个场景需要的是单向、长连接、持续状态更新，SSE 正好匹配。

---

## 15. `flushHeaders()` 是什么？

**回答要点：**

- 它不是新增 header。
- 它是把前面已经设置好的响应头立刻发给浏览器。
- 这样浏览器会马上把连接当成 SSE 流，而不是普通 HTTP 响应。

**这里实际发出去的头：**

```http
Content-Type: text/event-stream
Cache-Control: no-cache, no-transform
Connection: keep-alive
```

**一句话版：**

> `flushHeaders()` 的作用是立刻建立事件流连接，不是写 header 内容本身。

---

## 16. 前端为什么要同时展示 PR 信息、Diff Viewer、Timeline 和最终结果？

**回答要点：**

- PR 信息告诉用户对象是什么。
- Diff Viewer 提供原始证据。
- Timeline 展示模型怎么一步步分析。
- 最终结果给出风险和建议。
- 四块一起放，是为了让用户看到完整的“输入、过程、输出”。

**一句话版：**

> 这不是堆信息，而是把 review 的证据链完整呈现出来。

---

## 17. 这个项目的整体设计流程是什么？

**回答要点：**

可以按 git 演进来讲：

1. **先搭骨架**
   - 初始化项目结构
   - 跑通前后端

2. **先拿数据**
   - 解析 PR URL
   - 获取 PR metadata
   - 获取 changed files

3. **再治理上下文**
   - 构建 `Review Context`
   - 过滤低价值文件
   - 截断超长 patch
   - 加 warning 标记

4. **再收住模型行为**
   - 设计受控 `review loop`
   - 限制 action 白名单
   - 后端做结构化校验

5. **最后做实时展示**
   - 用 SSE 推送 step、heartbeat、loop_action、result
   - 前端用 EventSource 接收并渲染 timeline

**一句话版：**

> 这个项目是按“数据接入 -> 上下文治理 -> 受控 review loop -> SSE 实时可视化”四步演进出来的。

---

## 18. 一句总结这个项目

> 这个项目不是简单地接入 AI，而是把 PR Review 做成了一条可控、可解释、可回放的工程链路。

---

## 19. 你这个项目最核心的技术难点是什么？

**回答要点：**

- 最核心的难点不是调通模型 API，而是**让模型输出变得可控、可校验、可解释**。
- PR Review 本身是开放任务，模型容易直接总结、跳步骤、编造风险。
- 所以后端把模型行为约束成四类 action：
  - `inspect_files`
  - `record_risk`
  - `record_suggestion`
  - `finish`
- 每一轮 action 都要经过解析和校验，只有合法 action 才能进入状态。
- 前端再把这些 action 渲染成 Timeline，让用户看到模型每一步做了什么。

**一句话版：**

> 这个项目最难的不是接 AI，而是把不稳定的 AI 输出收敛成稳定的工程流程。

---

## 20. 为什么要设计 Review Loop，而不是一次模型调用直接输出结果？

**回答要点：**

- 一次调用容易变成黑盒总结，用户不知道模型看了哪些文件。
- Review Loop 可以把审查过程拆成多轮：
  - 先选文件
  - 再记录风险
  - 再给建议
  - 最后总结
- 每一轮的中间状态都可以校验和展示。
- 如果模型想跳过检查直接给风险，后端可以拒绝。
- 如果有 risk 但没有 suggestion，后端可以要求继续补建议。

**一句话版：**

> Review Loop 的价值是把“直接生成答案”改成“按规则完成审查过程”。

---

## 21. `parseReviewLoopAction` 主要解决什么问题？

**回答要点：**

- 它负责把模型返回的文本变成系统可执行的结构化 action。
- 它先尝试解析 JSON，如果模型外面包了自然语言，也会截取 `{}` 中的 JSON。
- 解析后会做严格校验：
  - `type` 必须合法
  - 必填字段不能为空
  - 文件必须存在于 PR 上下文中
  - 被忽略文件不能被检查
  - 未 inspected 的文件不能记录风险
  - suggestion 必须引用已有 riskId
- 这层是模型输出进入业务状态前的防线。

**一句话版：**

> `parseReviewLoopAction` 是模型输出和业务状态之间的安全闸门。

---

## 22. 如果模型输出的 JSON 不标准怎么办？

**回答要点：**

- 后端不会直接信任模型输出。
- 当前实现会先对完整文本做 `JSON.parse`。
- 如果失败，会尝试截取第一个 `{` 到最后一个 `}` 之间的内容再解析。
- 如果仍然失败，就抛出错误，不让非法内容进入 review 结果。
- 这样可以兼容模型偶尔在 JSON 外面加解释文字的情况，但不会无底线容错。

**一句话版：**

> 可以容忍模型多说几句，但不能容忍它输出不可执行的结构。

---

## 23. 为什么 `record_risk` 必须基于已 inspected 的文件？

**回答要点：**

- 这是为了防止模型基于文件名或经验编造风险。
- 如果一个文件没有被检查，模型就没有足够证据说明那里有问题。
- 后端通过 `assertInspectedFile` 校验这一点。
- 这样 risk 必须和实际看过的 patch 建立关系。
- 这能降低幻觉，也让 Timeline 的证据链更可信。

**一句话版：**

> 没看过的文件不能报风险，这是为了让每条风险都有审查依据。

---

## 24. 为什么 suggestion 必须绑定 riskId？

**回答要点：**

- Review 建议应该服务于具体风险，而不是泛泛而谈。
- 如果 suggestion 不绑定 risk，就很容易出现“建议很对，但和这个 PR 没关系”的情况。
- 通过 `riskId` 绑定，后端可以校验：
  - 这个 risk 是否存在
  - suggestion 是否对应已确认的问题
  - 最终结果能否把风险和修复建议配对展示
- 这样前端也可以把风险和建议合并成更清晰的 review 结果。

**一句话版：**

> 建议必须有对应风险，否则就是脱离上下文的空泛建议。

---

## 25. `shouldChallengeNoFindingFinish` 是为了解决什么问题？

**回答要点：**

- 它是为了防止模型过早结束，并且直接说“没有风险”。
- 如果模型已经检查了一些文件，但 0 risk 就想 finish，后端会插入一次 challenge。
- challenge 会提醒模型重新检查：
  - 错误处理
  - 参数校验
  - 测试缺失
  - API 契约变化
  - 状态顺序问题
- 如果复查后仍然没有发现问题，模型可以再次 finish。

**一句话版：**

> 这是对“太草率地说没问题”的二次审查机制。

---

## 26. `shouldChallengeMissingSuggestionsFinish` 是为了解决什么问题？

**回答要点：**

- 它解决的是“发现了风险但没有给修复建议”的问题。
- 如果 state 里存在 risk，但没有对应 suggestion，后端不会直接接受 finish。
- 后端会把缺少建议的 risks 放进 tool message，要求模型继续生成 suggestion。
- 这样最终结果不会只有问题没有解决方案。

**一句话版：**

> 有风险就必须尽量给建议，否则这个 review 对开发者的帮助不完整。

---

## 27. Review Loop 为什么要设置最大轮数？

**回答要点：**

- 模型可能重复检查、重复生成、或者迟迟不 finish。
- 最大轮数可以避免接口一直运行，控制成本和响应时间。
- 当前系统还设置了最大 risk 数、最大 suggestion 数、单轮最多检查文件数。
- 这些限制共同保证 Review Loop 可控。
- 如果达到限制，会返回 fallback summary，至少保留已经找到的风险和建议。

**一句话版：**

> 最大轮数是为了防止 AI 流程失控，也是成本和体验边界。

---

## 28. `buildReviewContext` 为什么要过滤 lock 文件和构建产物？

**回答要点：**

- lock 文件、dist、build、minified 文件通常信息量低，且内容很长。
- 如果直接喂给模型，会占用大量上下文。
- 这些文件还容易干扰模型，让模型把注意力放在低价值变化上。
- 所以后端通过 ignore pattern 标记 ignored 文件，并清空 patch。
- 同时把 ignored 信息放进 warning，让用户知道系统做了过滤。

**一句话版：**

> 过滤不是丢信息，而是把上下文预算留给真正值得 review 的代码。

---

## 29. 为什么单个 patch 要截断到固定长度？

**回答要点：**

- GitHub PR 里单个文件 patch 可能非常长。
- 太长会带来三个问题：
  - token 成本高
  - 模型注意力被稀释
  - 响应速度变慢
- 当前实现限制单文件 patch 最大字符数，并追加 `[Patch truncated for review context]`。
- 这样模型知道内容被截断，最终 summary 也可以体现覆盖范围限制。

**一句话版：**

> 截断是为了控制上下文和成本，同时明确告诉模型这不是完整文件。

---

## 30. 大 PR 怎么处理？为什么不能和小 PR 一样全量检查？

**回答要点：**

- 大 PR 文件多、diff 长，全量检查容易超上下文，也会让结果变慢。
- 系统会通过 changed files 数和 additions/deletions 判断 large PR。
- 大 PR 会加 warning，提示模型按风险优先级选择文件。
- Review Loop 中 `inspect_files` 也有限制，避免一次塞太多文件。
- 最终结果应该说明审查覆盖范围，而不是假装已经完整覆盖所有改动。

**一句话版：**

> 大 PR 的策略是风险优先，而不是机械地追求全量读取。

---

## 31. `changedModules` 是怎么来的？为什么只取前几个？

**回答要点：**

- 当前实现从未忽略文件的路径中取第一层目录作为模块名。
- 然后用 `Set` 去重，最多展示前 5 个。
- 这样可以快速告诉用户这个 PR 主要影响哪些区域。
- 它不是精确的架构模块识别，而是一个轻量的概览。
- 面试时要承认这是启发式方法，不要说成完整依赖分析。

**一句话版：**

> `changedModules` 是基于路径的一层粗粒度影响范围概览。

---

## 32. 前端为什么同时发起 Review Stream 和普通 PR 数据请求？

**回答要点：**

- Review Stream 用来实时展示 AI 审查进度。
- 普通 PR 数据请求用来渲染左侧 PR 信息和中间 Diff Viewer。
- 两者职责不同：
  - Stream 负责过程和结果
  - metadata/files/context 负责页面基础信息和证据展示
- 这样用户不用等 AI 完成，就能先看到 PR 基本信息和 patch。

**一句话版：**

> Stream 负责实时过程，普通请求负责页面基础数据，两者并行提升体验。

---

## 33. 前端如何处理 SSE 事件？

**回答要点：**

- 前端通过 `EventSource` 连接 `/review/stream`。
- 后端统一用 `data: JSON\n\n` 推送事件。
- 前端在 `onmessage` 里解析 `event.data`，根据 `payload.type` 分发：
  - `step` 更新 pipeline step
  - `loop_action` 追加到 Timeline
  - `heartbeat` 更新运行状态和耗时
  - `result` 设置最终结果
  - `done` 关闭连接
  - `error` 设置错误状态

**一句话版：**

> 前端用一个 SSE 连接接收事件，再按 type 分发到不同 UI 状态。

---

## 34. `heartbeat` 为什么是 8 秒一次？

**回答要点：**

- 它的目标不是高频刷新，而是告诉前端“任务还活着”。
- AI Review 可能要多轮模型调用，中间长时间没有业务事件。
- 8 秒一次可以兼顾：
  - 用户感知
  - 网络开销
  - 服务端压力
- 前端收到 heartbeat 后更新提示语和 elapsedSeconds。

**一句话版：**

> heartbeat 是长任务保活和可观测性信号，不需要像动画一样高频。

---

## 35. 如果 SSE 连接失败，前端怎么处理？

**回答要点：**

- 当前前端在 `eventSource.onerror` 中关闭连接。
- 同时把 `isReviewRunning` 置为 false，并设置错误文案。
- 这样用户不会一直看到“运行中”的假状态。
- 更完善的版本可以增加重试、断点恢复、后端 jobId 和历史事件缓存。
- 目前实现更偏简单直观，适合项目第一版。

**一句话版：**

> 当前版本失败就收口并提示，后续可以用 jobId 做可恢复任务。

---

## 36. Patch Viewer 为什么使用 `dangerouslySetInnerHTML`？安全吗？

**回答要点：**

- Patch Viewer 使用 highlight.js 对代码片段做语法高亮，高亮结果是 HTML 字符串。
- 所以前端用 `dangerouslySetInnerHTML` 插入高亮后的内容。
- 风险是：如果高亮输入里存在恶意 HTML，而输出没有正确转义，就可能有 XSS 风险。
- highlight.js 通常会转义代码内容，但更严谨的做法是：
  - 确认版本和配置
  - 禁止渲染任意 HTML
  - 对输出再做 sanitizer
  - 或改用更安全的 React AST 渲染方案
- 面试时不能只说“没问题”，要承认这是安全敏感点。

**一句话版：**

> 这里是为了代码高亮使用 HTML 注入，但它是安全敏感点，需要依赖高亮库转义或额外 sanitize。

---

## 37. 为什么 Patch Viewer 要按行判断 `+`、`-` 和 `@@`？

**回答要点：**

- Git patch 里：
  - `@@` 表示 hunk header
  - `+` 表示新增行
  - `-` 表示删除行
  - 其他是上下文行
- 前端按行分类，可以给不同类型的行上不同背景色。
- 这样用户能快速区分新增、删除和上下文。
- 对新增/删除行，代码高亮时会去掉开头符号，只高亮真实代码内容。

**一句话版：**

> 这是把原始 patch 转成可读 diff 视图的最小实现。

---

## 38. 这个项目为什么要前端展示 Timeline？

**回答要点：**

- AI Review 最大的问题之一是黑盒。
- 如果只给最终结果，用户不知道模型是否真的检查了关键文件。
- Timeline 展示每一轮 action：
  - 检查了哪些文件
  - 为什么检查
  - 发现了什么风险
  - 给了什么建议
- 这能增强用户对结果的信任，也方便发现模型哪里走偏。

**一句话版：**

> Timeline 是把 AI 的推理过程产品化，让用户能审查 AI 的审查过程。

---

## 39. 这个项目有哪些明显不足？

**回答要点：**

- 目前还不是完整的工程级 PR Review 平台。
- 明显不足包括：
  - 风险没有精确绑定 diff 行号
  - 没有 Review History
  - SSE 失败后不能恢复任务
  - 大 PR 覆盖率有限
  - 没有多模型策略
  - Patch Viewer 的 HTML 高亮需要继续加固安全
- 但当前版本已经跑通了核心闭环：PR 数据获取、上下文治理、受控 Review Loop、实时可视化。

**一句话版：**

> 当前版本验证了核心链路，但离生产级 review 平台还差行级定位、任务恢复、历史记录和安全加固。

---

## 40. 如果让你继续优化这个项目，你会先做什么？

**回答要点：**

- 我会优先做三件事：
  1. **行级定位**：risk 和 suggestion 绑定到具体 patch 行，提升可操作性。
  2. **任务持久化**：引入 jobId，SSE 断开后可以恢复 Timeline 和结果。
  3. **结果导出**：支持 Markdown Review Report 或 GitHub comment。
- 这三件事直接提升产品可用性，而不是只做技术炫技。
- 如果继续深入，可以加多模型策略和缓存。

**一句话版：**

> 我会先补行级定位、任务恢复和报告导出，因为它们最直接影响真实使用价值。

---

## 41. 如果面试官问你“这不就是套壳 AI 吗”，你怎么回答？

**回答要点：**

- 我会先承认项目用了大模型，但核心工作不是简单调用模型。
- 普通套壳是：输入 PR URL，拼 prompt，输出总结。
- 我的项目多了几层工程设计：
  - GitHub PR 数据获取
  - Review Context 构建
  - 低价值文件过滤和 patch 截断
  - 受控 action 协议
  - 后端状态机和校验
  - SSE 实时可视化
- 所以模型只是推理组件，系统价值在于把 review 流程工程化。

**一句话版：**

> 模型只是能力来源，这个项目的重点是把模型能力包装成可控、可解释的 review 系统。

---

## 42. 你怎么证明这个项目有工程复杂度？

**回答要点：**

- 可以从四个层面证明：
  1. **数据层**：解析 GitHub PR URL，获取 metadata 和 changed files。
  2. **上下文层**：过滤低价值文件、截断长 patch、标记 warning。
  3. **推理层**：Review Loop、多轮 action、状态约束、输出校验。
  4. **交互层**：SSE、heartbeat、Timeline、Diff Viewer、最终结果展示。
- 这些模块之间有明确边界，不是一个页面里拼 prompt。

**一句话版：**

> 它的复杂度来自数据治理、模型约束、实时通信和可视化流程的组合。

---

## 43. 这个项目里前端状态为什么没有用 Redux？

**回答要点：**

- 当前项目的状态主要集中在一个页面内：
  - PR metadata
  - changed files
  - selected file
  - review steps
  - loop actions
  - review result
  - error/loading 状态
- 状态虽然多，但跨页面共享不复杂。
- 用 React `useState` 可以更直接，减少样板代码。
- 如果后续加入历史记录、多页面、用户系统或任务中心，再考虑 Zustand/Redux 更合适。

**一句话版：**

> 当前状态复杂但作用域集中，用本地状态够用；状态跨页面后再引入全局 store。

---

## 44. `EventSource` 有什么限制？

**回答要点：**

- `EventSource` 默认只能发 GET 请求，不适合复杂 request body。
- 浏览器对同域 SSE 连接数有限制。
- 它是单向通信，客户端不能通过同一连接向服务端发消息。
- 某些代理可能缓冲响应，需要设置 `no-transform` 和正确的流式响应头。
- 断线重连虽有原生支持，但如果后端没有 jobId 和事件缓存，重连后也无法恢复任务状态。

**一句话版：**

> EventSource 很适合单向进度推送，但不适合复杂双向通信和可恢复任务。

---

## 45. 为什么这里不用 WebSocket？

**回答要点：**

- 这个场景前端主要是接收后端推送，不需要高频双向通信。
- SSE 更轻量，基于 HTTP，部署和调试都简单。
- 后端只要持续 `res.write`，前端用 `EventSource` 接收即可。
- WebSocket 更适合协同编辑、聊天室、游戏这类双向实时场景。
- 当前项目用 WebSocket 会增加连接管理和协议复杂度。

**一句话版：**

> 这个项目是后端单向推送 review 进度，SSE 的复杂度和能力刚好匹配。

---

## 46. 你怎么处理 GitHub API 失败？

**回答要点：**

- 当前请求失败会在路由或前端抛出错误并展示错误信息。
- 如果是面试回答，可以分现状和优化：
  - 现状：失败提示用户，不进入 review 流程。
  - 优化：区分 404、403、rate limit、网络错误。
  - 对 rate limit 可以提示用户配置 token。
  - 对临时网络错误可以重试。
  - 对已获取的数据可以做缓存。

**一句话版：**

> 当前版本是失败即提示，生产级版本要区分错误类型、重试和缓存。

---

## 47. 如果 PR 很大，模型没有看完所有文件，结果还可信吗？

**回答要点：**

- 不能说完全可信，只能说是“在已检查范围内可信”。
- 大 PR 场景下，系统会优先检查高风险文件。
- 最终结果应该说明覆盖范围和限制。
- 这也是为什么 large PR warning 很重要。
- 未来可以加入覆盖率展示，例如 inspected files / included files。

**一句话版：**

> 大 PR 的结果应该带覆盖范围，不能假装全量审查过。

---

## 48. 这个项目怎么降低模型幻觉？

**回答要点：**

- 主要靠工程约束，而不是靠“提示词写得好”。
- 具体包括：
  - 只能检查 context 里的真实文件
  - ignored 文件不能被检查
  - 未 inspected 文件不能记录 risk
  - suggestion 必须绑定已有 risk
  - riskLevel 必须是固定枚举
  - 0 risk 直接 finish 会触发二次检查
- 这些规则把模型限制在可验证的上下文里。

**一句话版：**

> 降低幻觉靠白名单、状态约束和后端校验，而不是单纯相信 prompt。

---

## 49. 你怎么设计 prompt 才能让模型输出固定 action？

**回答要点：**

- prompt 要明确说明模型当前的角色、可用上下文和允许的动作。
- 必须要求模型每轮只输出一个 JSON action。
- 要把 action schema 写清楚。
- 还要告诉模型禁止事项：
  - 不能检查 ignored 文件
  - 不能对未 inspected 文件记录风险
  - 不能输出自然语言解释
  - risk 必须有具体文件依据
- 但 prompt 只是第一层约束，后端校验才是最终保障。

**一句话版：**

> prompt 负责引导，后端校验负责兜底。

---

## 50. 如果模型一直输出非法 action，系统会怎样？

**回答要点：**

- 当前 `parseReviewLoopAction` 会抛错，流程会失败。
- 这能保证非法结果不会进入最终 review。
- 更完善的版本可以增加：
  - 非法 action 重试
  - 把错误信息反馈给模型要求修正
  - 超过重试次数后返回部分结果
  - 记录错误日志用于调 prompt
- 面试中要说明：当前版本更注重约束正确性，容错还可以继续增强。

**一句话版：**

> 当前非法输出会失败收口，后续可以做“错误反馈 + 有限重试”。

---

## 51. 你项目里的“可解释性”体现在哪里？

**回答要点：**

- 可解释性不是让模型解释自己，而是让系统展示它做过什么。
- 本项目的可解释性体现在：
  - Review Context 显示输入来源
  - Diff Viewer 显示原始 patch
  - Timeline 显示每轮 action
  - risk 绑定文件和原因
  - suggestion 绑定 risk
- 用户可以从最终结论回溯到模型检查过程和原始代码变更。

**一句话版：**

> 可解释性就是让用户能从结论追到过程，再追到原始 diff。

---

## 52. 你项目里的“可控性”体现在哪里？

**回答要点：**

- 可控性体现在模型不能自由输出任意内容。
- 它只能在固定 action 协议内行动。
- 后端限制了：
  - 每轮一个 action
  - 单轮最多检查文件数
  - 最大迭代次数
  - 风险和建议数量
  - 文件、riskId、riskLevel 的合法性
- 这些限制让 AI Review 从自然语言生成变成受控流程。

**一句话版：**

> 可控性就是把模型从“自由发挥”限制到“按协议执行”。

---

## 53. 你项目里的“上下文感知”体现在哪里？

**回答要点：**

- 系统不是只拿 PR 标题问模型。
- 它会获取完整 PR metadata、changed files 和 patch。
- Review Context 还包含：
  - base/head 分支
  - additions/deletions
  - 文件状态
  - ignored/truncated 信息
  - large PR warning
- 模型基于这个结构化上下文做 review，而不是凭经验猜。

**一句话版：**

> 上下文感知就是让模型基于真实 PR 结构和 patch 做判断。

---

## 54. 这个项目如果要上线给团队用，还需要补什么？

**回答要点：**

- 需要补四类能力：
  1. **权限和安全**：GitHub OAuth、私有仓库权限、token 安全。
  2. **任务系统**：jobId、任务状态、失败重试、历史记录。
  3. **结果质量**：行级定位、误报反馈、模型评估集。
  4. **协作集成**：生成 GitHub Review Comment、导出 Markdown、团队配置规则。
- 当前版本更像验证核心技术链路的 MVP。

**一句话版：**

> 团队级上线要补权限、任务、质量评估和 GitHub 协作闭环。

---

## 55. 你怎么向面试官讲这个项目的架构？

**回答要点：**

可以按四层讲：

1. **前端层**
   - React + TypeScript
   - PR 输入、Diff Viewer、Timeline、Result 展示

2. **接口层**
   - Express routes
   - `/api/github/*`
   - `/api/review/context`
   - `/review/stream`

3. **上下文层**
   - GitHub API 获取 metadata/files
   - buildReviewContext 过滤、截断、统计、warning

4. **AI Review 层**
   - RightCode API
   - Review Loop
   - action parser
   - state validation
   - SSE 推送

**一句话版：**

> 架构上是前端可视化、Express API、Review Context 构建、受控 AI Loop 四层。

---

## 56. 如果让你重构这个项目，你会怎么拆模块？

**回答要点：**

- 我会继续保持现有分层，但把几个边界拆得更清楚：
  - GitHub client 只负责外部数据获取
  - Context builder 只负责数据治理
  - Review loop 只负责状态机和动作执行
  - SSE sender 只负责事件协议
  - 前端 Timeline 只负责展示 action
- 还会把 PipelineEvent 抽成共享类型，避免前后端类型漂移。
- 复杂后可以把 review job 独立成服务。

**一句话版：**

> 重构方向是进一步强化边界，把数据获取、上下文治理、AI 状态机和事件推送拆清楚。

---

## 57. 这个项目有什么可以量化的结果？

**回答要点：**

- 如果没有真实用户数据，不要硬编业务指标。
- 可以说工程指标：
  - 支持真实 GitHub PR URL 解析
  - 支持 metadata、changed files、patch 获取
  - 支持多轮 Review Loop
  - 单轮最多检查 7 个文件
  - 单文件 patch 最多保留 10000 字符
  - heartbeat 每 8 秒推送一次
  - 大 PR 阈值：20 个文件或 800 行变更
- 面试中要强调这些是系统边界指标，不是业务增长指标。

**一句话版：**

> 没有真实用户数据时，就讲清楚系统能力边界和工程约束指标。

---

## 58. 如果被问到“你在项目里主要负责什么”，怎么回答？

**回答要点：**

- 可以按模块说，不要只说“我负责前端/后端”。
- 更好的回答：
  - 我负责从 PR URL 到 Review 结果展示的完整链路。
  - 后端包括 GitHub PR 数据获取、Review Context 构建、Review Loop、SSE 推送。
  - 前端包括 PR 信息展示、Diff Viewer、Timeline 和 Review Result。
  - 重点难点是模型 action 协议和后端校验规则。
- 如果是个人项目，也可以直接说“这是我独立完成的项目”。

**一句话版：**

> 我负责的是完整 AI Review Pipeline，而不是单独某个页面或接口。

---

# 简历项目专项面试题

下面的问题围绕简历中的两个项目：AI Agent 对话助手、Project Sekai 桌面宠物。回答重点是“先说结论，再讲实现，再讲取舍和不足”。

## 59. 你的 AI Agent 对话助手和普通聊天页面有什么区别？

**回答要点：**

- 普通聊天页面主要是用户发消息、模型返回文本。
- 我的项目除了基础对话，还支持多轮 Agent Loop 和工具调用过程展示。
- 后端会处理模型返回的 `tool_call`，执行工具后把 `tool_result` 再交给模型二次推理。
- 前端不仅展示最终 answer，还展示 thinking、tool_call、tool_result、responding 等阶段。
- 所以它更像一个可视化的 Agent 执行器，而不是简单聊天 UI。

**一句话版：**

> 普通聊天页面只展示回答，我这个项目展示了 Agent 从思考、调工具到生成答案的完整过程。

---

## 60. 为什么 AI 对话项目使用 SSE，而不是 WebSocket？

**回答要点：**

- 这个场景主要是服务端向前端持续推送模型输出，通信方向以单向为主。
- SSE 基于 HTTP，浏览器原生支持，接入成本比 WebSocket 低。
- 模型流式回复、工具调用状态、Agent 阶段状态都适合通过 SSE 持续推送。
- WebSocket 更适合强双向实时通信，比如 IM、游戏、协同编辑。
- 但 SSE 也有局限，比如连接数限制、代理缓冲、断线恢复能力有限。

**一句话版：**

> AI 回复流是典型服务端单向推送场景，用 SSE 比 WebSocket 更轻。

---

## 61. 你前端是怎么解析 SSE 流的？

**回答要点：**

- 前端通过 `fetch` 获取响应体，再用 `response.body.getReader()` 读取流。
- 使用 `TextDecoder` 把二进制 chunk 解码成字符串。
- 因为 chunk 边界不等于事件边界，所以需要维护 buffer。
- 按 SSE 的空行分隔符 `\n\n` 拆出完整事件，再解析 `event:` 和 `data:`。
- 最后根据事件类型分发到不同回调，比如 text、tool_call_start、tool_result、agent_status。

**一句话版：**

> 核心是“流式读取 + buffer 拼接 + 按 SSE 事件边界解析 + 按事件类型分发”。

---

## 62. 为什么不用 `EventSource`，而是用 fetch 读取流？

**回答要点：**

- `EventSource` 默认只能发 GET 请求。
- AI 对话一般要发送 messages、sessionId、配置等复杂 body。
- 使用 fetch 可以走 POST 请求，更适合聊天接口。
- fetch 也可以配合 `AbortController` 实现停止生成。
- 代价是需要自己写 SSE 解析逻辑。

**一句话版：**

> fetch 适合带 body 的聊天请求，但需要自己处理流式解析。

---

## 63. Agent Loop 的完整流程是什么？

**回答要点：**

- 用户消息进入后端，后端构造 system prompt 和历史消息。
- 模型流式返回内容，如果返回普通文本，就直接推给前端。
- 如果返回 `tool_calls`，后端会收集工具名和参数。
- 后端执行工具，把结果作为 `tool` 消息追加进上下文。
- 再次调用模型，让模型基于工具结果继续推理。
- 这个过程有最大轮次限制，避免无限循环。

**一句话版：**

> Agent Loop 就是“模型决定调工具，后端执行工具，工具结果再喂回模型继续推理”。

---

## 64. 你项目里的工具调用如何保证安全？

**回答要点：**

- 工具不是模型想调什么就调什么，而是后端白名单注册。
- 工具有固定 definition，包括 name、description、parameters。
- 执行前会解析 JSON 参数，未知工具会返回错误。
- 文件读取工具限制在工作区内，并禁止读取 `.env`、密钥、node_modules、dist、build 等路径。
- 计算器工具也限制表达式字符，避免执行任意代码。

**一句话版：**

> 工具调用必须白名单、参数校验、路径限制，不能把系统能力完全交给模型。

---

## 65. 工具调用的前端状态怎么设计？

**回答要点：**

- 每条 assistant 消息可以带 `tool_calls` 和 `tool_results`。
- 工具状态包括 pending、success、error。
- 前端收到 `tool_call_start` 时创建工具调用项。
- 收到 `tool_call_delta` 时持续拼接参数。
- 收到 `tool_result` 时更新结果、成功状态和耗时。
- 这样用户能看到工具调用从开始、参数生成、执行结果到完成的全过程。

**一句话版：**

> 工具调用状态不是单独弹窗，而是挂在 assistant 消息里形成完整执行链。

---

## 66. 长对话为什么要做上下文压缩？

**回答要点：**

- 长对话会不断增加 token 成本和请求延迟。
- 如果每次都把完整历史发给模型，成本和上下文长度都会失控。
- 项目里超过一定消息数后，会把较早消息压缩成摘要。
- 同时保留最近几条消息，保证当前对话连贯。
- 这样在成本、速度和上下文连续性之间做平衡。

**一句话版：**

> 长对话压缩是为了控制 token 成本，同时尽量保留对话连续性。

---

## 67. 流式输出时频繁更新 React 状态会有什么问题？

**回答要点：**

- 如果每个 token 都触发一次 `setState`，会导致频繁渲染。
- 长回答场景下可能出现输入卡顿、滚动卡顿、Markdown 反复解析。
- 优化方向包括：
  - 用 buffer 合并 token
  - 用节流或 `requestAnimationFrame` 批量刷新
  - 只更新最后一条 assistant 消息
  - 消息组件做 memo
  - 长列表用虚拟滚动
- 当前项目已经按最后一条 assistant 消息更新，后续可以继续做批量 flush。

**一句话版：**

> 流式输出的性能关键是减少无意义重渲染，而不是每个 token 都刷新全页面。

---

## 68. Markdown 和代码高亮有什么安全风险？

**回答要点：**

- 模型输出本质上是不可信内容。
- 如果允许原始 HTML 或直接插入 HTML，可能产生 XSS。
- Markdown 渲染时要谨慎处理 raw HTML、链接、代码高亮输出。
- 更稳妥的做法是禁用 raw HTML，必要时使用 sanitizer。
- 代码复制功能也要从安全的文本节点或解析结果里取内容，避免复制到额外注入内容。

**一句话版：**

> AI 输出不能直接信任，Markdown 渲染要防 XSS。

---

## 69. 你为什么用 Zustand，而不是 Redux？

**回答要点：**

- 当前项目状态主要围绕会话、消息、生成状态和 Agent 状态。
- Zustand 更轻量，样板代码少，适合中小型应用。
- 使用 store 可以避免多层 props 传递。
- 如果后续项目变复杂，比如多页面、复杂权限、团队协作、状态审计，Redux 会更合适。
- 选型重点不是谁更高级，而是当前复杂度下哪种更合适。

**一句话版：**

> 这个项目状态复杂但业务域集中，Zustand 更轻量够用。

---

## 70. AI 对话项目里你最容易被追问的不足是什么？

**回答要点：**

- 当前项目还不算生产级 AI 应用。
- 明显不足包括：
  - 缺少严格鉴权和用户体系
  - 工具权限控制还可以更细
  - 流式更新性能还可以继续优化
  - Markdown 安全策略需要补强
  - Agent Loop 缺少更完整的日志和观测
  - 没有持久化数据库，只是本地会话存储
- 面试时要主动承认边界，再讲下一步优化方案。

**一句话版：**

> 当前版本跑通了 Agent 对话闭环，但离生产级还差权限、安全、观测和持久化。

---

## 71. 你的桌面宠物项目和普通 React Web 项目有什么区别？

**回答要点：**

- 普通 React 项目主要运行在浏览器沙箱里。
- Electron 项目有主进程、渲染进程和 preload 三层。
- 窗口控制、系统托盘、置顶、鼠标穿透、本地配置都属于桌面端能力。
- 这些能力不能直接暴露给前端页面，需要通过受限 IPC 调用。
- 所以它比普通 Web 项目多了进程通信、安全边界和系统能力封装。

**一句话版：**

> Electron 项目的难点不是 React 页面，而是桌面能力和安全边界。

---

## 72. Electron 里的 main、preload、renderer 分别负责什么？

**回答要点：**

- main 是主进程，负责创建窗口、托盘、窗口控制、系统 API。
- renderer 是渲染进程，负责 React UI 和用户交互。
- preload 是两者之间的安全桥梁。
- renderer 不应该直接拿 Node 能力，而是通过 preload 暴露受限 API。
- 你的项目里 `window.pet` 就是通过 `contextBridge` 暴露的受限接口。

**一句话版：**

> main 管系统，renderer 管界面，preload 管安全通信边界。

---

## 73. Electron 项目怎么保证安全？

**回答要点：**

- 开启 `contextIsolation`，让页面上下文和 preload 隔离。
- 开启 `sandbox`，降低渲染进程能力。
- 不把 `ipcRenderer` 原样暴露给前端。
- preload 只暴露受控的 `window.pet` API。
- IPC channel 用枚举统一管理，避免随意调用任意 channel。
- 主进程负责真正的窗口和设置操作。

**一句话版：**

> Electron 安全的核心是不给渲染进程直接 Node 权限，只暴露最小白名单 API。

---

## 74. 为什么不能直接在 renderer 里调用系统能力？

**回答要点：**

- renderer 负责展示页面，可能承载不可信内容或被 XSS 攻击。
- 如果 renderer 直接拥有 Node 或系统能力，风险会被放大。
- 一旦页面被注入脚本，攻击者可能读取文件、执行系统操作。
- 所以系统能力必须收敛到 main process。
- renderer 只能通过受控 IPC 请求特定能力。

**一句话版：**

> renderer 越像普通网页越安全，系统能力要放在主进程受控执行。

---

## 75. 你的桌宠拖拽是怎么实现的？

**回答要点：**

- 不是简单依赖 CSS 拖拽。
- renderer 监听 pointer 事件，超过拖拽阈值后通知主进程开始拖动。
- 主进程通过 `screen.getCursorScreenPoint()` 获取鼠标位置。
- 用定时器按接近 60 FPS 的频率更新窗口位置。
- 移动时还会限制窗口不能超出屏幕工作区。

**一句话版：**

> 拖拽是 renderer 识别交互，main 负责按鼠标位置移动真实窗口。

---

## 76. 鼠标穿透是怎么实现的？

**回答要点：**

- Electron 可以通过 `win.setIgnoreMouseEvents` 实现窗口忽略鼠标事件。
- 开启穿透后，鼠标可以操作窗口下面的应用。
- 但用户仍然需要和桌宠交互，所以项目会根据鼠标是否在宠物 hitbox 内动态切换。
- 菜单或角色选择器打开时，要强制关闭穿透，保证 UI 可点击。

**一句话版：**

> 鼠标穿透不是一直开，而是根据交互区域动态切换。

---

## 77. 桌宠状态机是怎么设计的？

**回答要点：**

- 状态包括 idle、tap-head、tap-body、wave、walk。
- 用户点击头部触发 tap-head，点击身体触发 tap-body。
- 也可以通过菜单触发 wave 和 walk。
- 每个动作有持续时间，结束后回到 idle。
- 同时维护台词队列，动作触发时展示对应台词。

**一句话版：**

> 桌宠状态机就是“动作触发 - 播放动画和台词 - 到时回到 idle”。

---

## 78. 你是怎么判断点击头部还是身体的？

**回答要点：**

- 点击时先拿到当前 sprite 在窗口里的实际位置和缩放尺寸。
- 计算鼠标相对 sprite 的坐标。
- 如果点击点不在 sprite 范围内，就忽略。
- 如果在范围内，再根据 `headHitRatio` 判断是在头部区域还是身体区域。
- 不同角色可以配置不同的 headHitRatio。

**一句话版：**

> 本质是把屏幕点击坐标换算成 sprite 内部坐标，再按比例划分头部和身体。

---

## 79. 为什么要把 GIF 转成 WebP 雪花图？

**回答要点：**

- 直接播放 GIF 不方便精确控制帧、动作切换和资源预加载。
- 雪花图加 JSON 帧表可以让前端自己控制 frameIndex。
- WebP 通常比 GIF 更适合现代图像资源。
- 构建脚本会把 2 个角色、12 个动作 GIF 转成 WebP 雪花图和 JSON 元数据。
- 运行时只需要根据帧表计算 background position。

**一句话版：**

> GIF 转雪花图是为了更稳定地控制动画播放和动作切换。

---

## 80. `useSpriteAnimation` 的原理是什么？

**回答要点：**

- Hook 内部维护当前帧 `frameIndex`。
- 根据 sprite 的 fps 计算 interval。
- 定时增加 frameIndex。
- 如果动作是 loop，就回到第一帧；否则停在最后一帧。
- 渲染时通过 `backgroundPosition` 显示雪花图中的对应帧。

**一句话版：**

> 它就是用定时器推进帧索引，再用 background-position 从雪花图里取当前帧。

---

## 81. 角色切换是怎么实现的？

**回答要点：**

- 资源构建后会生成 `index.json`，里面记录角色和动作资源。
- 前端加载 manifest 后得到可选角色列表。
- 用户在 CharacterPicker 里选择角色。
- 选择结果写入 electron-store 的 `selectedCharacterId`。
- 下次启动时读取本地配置恢复角色。

**一句话版：**

> 角色切换靠 manifest 管资源，electron-store 做选择持久化。

---

## 82. 桌宠项目里的系统托盘有什么作用？

**回答要点：**

- 桌宠窗口通常是无边框、跳过任务栏的。
- 如果没有托盘，用户可能找不到控制入口。
- 托盘提供 Show Pet、Always On Top、Click-Through、Quit 等操作。
- 它是桌面常驻应用的基础控制入口。

**一句话版：**

> 托盘是无边框桌面应用的兜底控制入口。

---

## 83. Electron 项目有哪些跨平台问题？

**回答要点：**

- Linux X11 和 Wayland 对透明窗口、置顶、鼠标穿透支持不同。
- macOS 需要处理 Dock、全工作区显示和窗口按钮隐藏。
- 不同平台的窗口行为不完全一致。
- 项目里会识别 session type，并把 Wayland 下透明或置顶行为视为可能降级。
- 面试时要说明：跨平台不是写一套代码就完全一致，需要识别平台差异。

**一句话版：**

> Electron 能跨平台，但窗口透明、置顶、穿透这些能力仍然有平台差异。

---

## 84. 你的 Electron 项目最大不足是什么？

**回答要点：**

- 当前项目更像桌宠交互和资源管线的 MVP。
- 不足包括：
  - 没有真正接入 AI 对话链路
  - 角色和动作资源仍然有限
  - 没有完整设置面板
  - 缺少打包后的跨平台验证
  - 没有自动更新和崩溃日志
- 但它已经覆盖 Electron 的核心能力：窗口、托盘、IPC、安全隔离、资源构建和状态机。

**一句话版：**

> 它目前是桌面宠物 MVP，不是完整 AI 桌面助手。

---

## 85. 如果面试官问“这个项目是不是玩具项目”，你怎么回答？

**回答要点：**

- 可以承认它不是商业级产品，但不是纯玩具。
- 它验证了 Electron 桌面端的关键工程问题：
  - 进程通信
  - 安全隔离
  - 窗口控制
  - 鼠标穿透
  - 资源构建
  - 状态机
  - 本地持久化
- 如果继续做产品化，可以接入 AI 对话、提醒、设置中心、打包发布和自动更新。

**一句话版：**

> 它不是商业级产品，但覆盖了桌面端开发的一组真实工程问题。

---

## 86. 两个项目放在简历里，你怎么串成一个技术故事？

**回答要点：**

- 可以说自己主要关注 AI 应用前端和复杂交互系统。
- AI Agent 对话助手体现的是：
  - 流式通信
  - Agent Loop
  - 工具调用
  - 消息状态管理
- 桌面宠物体现的是：
  - Electron 桌面端能力
  - IPC 安全边界
  - 动画状态机
  - 本地配置和资源管线
- 两个项目共同体现：不仅会写页面，也能处理状态、通信、进程边界和工程约束。

**一句话版：**

> 我的项目主线是复杂交互前端：一个偏 AI 流式应用，一个偏桌面端工程能力。
